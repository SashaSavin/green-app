// import { useLocation } from 'react-router-dom';
import { ChatLayout } from './components';

export const Chat = () => {
  // const { state } = useLocation();
  // const { user } = state;
  return (
    <div>
      <ChatLayout />
    </div>
  );
};
